export const math=(number,input)=>{
    console.log(number, input)
    switch (number) {
        case number='table':
            return input*2
        default:
            return 'give me the number'
            break;
            
    }
}